# firstlib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build firstlib` to build the library.

## Running unit tests

Run `nx test firstlib` to execute the unit tests via [Jest](https://jestjs.io).
